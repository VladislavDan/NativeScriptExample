<Page loaded="loaded">
    <ActionBar title="GitHub Repositories" class="action-bar"></ActionBar>
    <StackLayout orientation="vertical">
        <ListView items="{{ reposList }}" height="100%">
            <ListView.itemTemplate>
                <Label text="{{ name }}"/>
            </ListView.itemTemplate>
        </ListView>
    </StackLayout>
</Page>